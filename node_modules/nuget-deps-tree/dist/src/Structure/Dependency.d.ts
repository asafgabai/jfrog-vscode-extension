import { DependencyDetails } from '../../model';
export declare class Dependency implements DependencyDetails {
    _id: string;
    _version: string;
    constructor(_id: string, _version: string);
    get id(): string;
    set id(value: string);
    get version(): string;
    set version(value: string);
}
