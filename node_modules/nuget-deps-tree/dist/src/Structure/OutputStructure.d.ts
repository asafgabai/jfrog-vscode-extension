import { DependencyTree } from '../DependencyTree/Tree';
export declare class Tree {
    private _projects;
    constructor(_projects: Project[]);
    get projects(): Project[];
    set projects(value: Project[]);
    toJSON(): {
        projects: Project[];
    };
}
export declare class Project {
    private _name;
    private _dependencies;
    constructor(_name: string, _dependencies: DependencyTree[]);
    get name(): string;
    set name(value: string);
    get dependencies(): DependencyTree[];
    set dependencies(value: DependencyTree[]);
    toJSON(): {
        name: string;
        dependencies: DependencyTree[];
    };
}
