import { CaseInsensitiveMap } from '../../model';
export declare class NugetPackage {
    private _id;
    private _version;
    private _dependencies;
    constructor(_id: string, _version: string, nuspecContent: string);
    get dependencies(): CaseInsensitiveMap<boolean>;
    set dependencies(value: CaseInsensitiveMap<boolean>);
    get version(): string;
    set version(value: string);
    get id(): string;
    set id(value: string);
    /**
     * Fills the dependencies map of this nuget package from metadata found in nuspec file.
     * @param nuspecContent - Content read from nuspec file.
     */
    fillDependenciesMap(nuspecContent: string): void;
}
