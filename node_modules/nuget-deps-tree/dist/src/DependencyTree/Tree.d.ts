import { CaseInsensitiveMap, DependencyDetails } from '../../model';
export declare class DependencyTree {
    private _id;
    private _version;
    private _dependencies;
    constructor(_id: string, _version: string, allDependencies: CaseInsensitiveMap<DependencyDetails>, childrenMap: CaseInsensitiveMap<string[]>);
    get id(): string;
    set id(value: string);
    get version(): string;
    set version(value: string);
    get dependencies(): DependencyTree[];
    set dependencies(value: DependencyTree[]);
    toJSON(): {
        id: string;
        version: string;
        dependencies: DependencyTree[];
    };
    /**
     * Recursively add children nodes for a dependency.
     * @param allDependencies - All dependencies, direct and transitive.
     * @param children - All dependencies pointing to their children.
     */
    private addChildren;
}
