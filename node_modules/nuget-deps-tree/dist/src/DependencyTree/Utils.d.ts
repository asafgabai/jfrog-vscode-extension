import { Extractor } from '../../model';
import { DependencyTree } from './Tree';
declare type Root = DependencyTree[];
export declare const absentNupkgWarnMsg: string;
export declare class DependenciesUtils {
    /**
     * Creates dependency tree using the data received from the extractors.
     * @param extractor
     * @returns project's dependency tree
     */
    static createDependencyTree(extractor: Extractor): Root;
}
export {};
