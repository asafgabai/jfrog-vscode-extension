import { IDetailsResponse } from '../../model';
import { HttpClient } from '../HttpClient';
import { ILogger } from '../../model/';
export declare class XrayDetailsClient {
    private readonly httpClient;
    private readonly logger;
    constructor(httpClient: HttpClient, logger: ILogger);
    build(buildName: string, buildNumber: string, projectKey?: string): Promise<IDetailsResponse>;
}
