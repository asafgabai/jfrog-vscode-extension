import { ISummaryRequestModel, ISummaryResponse } from '../../model';
import { HttpClient } from '../HttpClient';
import { ILogger } from '../../model/';
export declare class XraySummaryClient {
    private readonly httpClient;
    private readonly logger;
    private readonly summaryComponentsEndpoint;
    constructor(httpClient: HttpClient, logger: ILogger);
    component(model: ISummaryRequestModel): Promise<ISummaryResponse>;
}
