import { IArtifactoryVersion, IUsageFeature } from '../../model';
import { HttpClient } from '../HttpClient';
import { ILogger } from '../../model/';
export declare class ArtifactorySystemClient {
    private readonly httpClient;
    private readonly logger;
    private readonly pingEndpoint;
    private readonly versionEndpoint;
    private readonly usageEndpoint;
    constructor(httpClient: HttpClient, logger: ILogger);
    ping(): Promise<boolean>;
    version(): Promise<IArtifactoryVersion>;
    reportUsage(userAgent: string, featureArray: IUsageFeature[]): Promise<string>;
}
