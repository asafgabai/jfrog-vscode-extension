import { HttpClient } from '../HttpClient';
import { IAqlSearchResult } from '../../model';
import { ILogger } from '../../model/';
export declare class ArtifactorySearchClient {
    private readonly httpClient;
    private readonly logger;
    private readonly aqlEndpoint;
    constructor(httpClient: HttpClient, logger: ILogger);
    aqlSearch(aqlQuery: string): Promise<IAqlSearchResult>;
}
